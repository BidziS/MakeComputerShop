@model MakeComputerShop.Bll.Dtos.MotherboardDto
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div class="container">
    @if (Model == null)
    {
        <text>
            Nie ma żadnych producentów w bazie
        </text>
    }
    else
    {
        <div class="item-homepage">
            <div class="row">
                @Html.Partial("_MotherboardDetails", Model)
                <div class="row">
                    <div class="block-center">
                        @Html.ActionLink("powrót", "All", "Motherboard", null, new { @class = "btn" })
                        @Html.ActionLink("do koszyka", "AddToShopCart", "Motherboard", new { id = Model.Id }, new { @class = "btn" })
                    </div>
                </div>
            </div>
        </div>
    }

</div>